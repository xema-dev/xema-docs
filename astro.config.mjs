import { defineConfig } from 'astro/config';

export default defineConfig({
  // Site config; carve scaffold only — full content site lands later
  site: 'https://xema-dev.github.io/xema-docs',
  base: '/xema-docs',
});
