name: CI
on:
  push:
    branches: [main]
  pull_request:
jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Phase O placeholder
        run: echo "Phase O lands the real CI pipeline here."
